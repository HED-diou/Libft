#include "libft.h"
#include <unistd.h>
void *ft_memset(void *ss, int c, size_t n)
{
	int i = 0;
	char *s = (char*)ss;
	
}
int main()
{
	ft_memset("hello world",'*',3);
	memset("hello world",'*',3);
}
